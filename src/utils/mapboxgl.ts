// Vite handles workers natively — no worker-loader needed.
// mapbox-gl >= 2.x bundles its own worker; no manual workerClass assignment required.
// This file is intentionally kept as a no-op import placeholder.
export {};
